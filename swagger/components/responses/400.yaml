description: Incorrect data
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Missing or incorrect data
        data:
          type: object
          properties:
            message:
              type: string
              example: Bad request