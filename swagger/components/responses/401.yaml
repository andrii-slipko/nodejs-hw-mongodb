description: Unauthorized
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 401
        message:
          type: string
          example: Unauthorized
        data:
          type: object
          properties:
            message:
              type: string
              example: Access token expired or incorrect